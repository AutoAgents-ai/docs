# Claude Working Note

Documentation process rules are centralized in `AGENTS.md`.

- Always follow `AGENTS.md` for release notes and product feature documentation updates.
- If any instruction here conflicts with `AGENTS.md`, `AGENTS.md` takes precedence.
