---
title: "创建评测任务"
---

### 操作流程

步骤 1：点击右上角【创建规则】按钮。
步骤 2（评测对象配置）：输入评测任务名称、描述，选择评测对象（智能体），设置评测时的并发数。
步骤 3（评测集配置）：选择评测数据集、数据范围（可选），选择用户输入（数据集中的哪个字段作为智能体的输入）、图片（可选）、附件（可选）。
步骤 4（规则与映射配置）：选择添加（单/多个）评测规则，针对规则中字段映射的参数选择数据来源。
步骤 5：选择右下角【创建】或【创建并运行】按钮
